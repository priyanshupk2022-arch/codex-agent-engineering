# Example: Secure Sandbox Deployment

Demonstrates configuring autonomous CI/CD workers running Codex CLI in Docker with `workspace-write` filesystem isolation, read-only host mounts, and blocked outbound network access.
