# Example: MCP Tool Integration

Demonstrates connecting Codex to a local PostgreSQL MCP server and a GitHub issue tracker MCP server using `.codex/config.toml` with strict sandbox permission boundaries.
